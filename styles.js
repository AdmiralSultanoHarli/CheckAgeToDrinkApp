import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
      marginTop: 40,
      padding: 20,
      flex: 1,
      backgroundColor: '#fff',
    },
    input: {
      backgroundColor: "#2AADEC",
      padding: 20,
      borderWidth: 2,
      marginTop: 10
    },
    button_style: {
        marginTop: 20
    }
  });

export default styles;